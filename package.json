{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./build",
    "start": "bun run ./build/index.js",
    "prepare": "husky"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7"
  }
}
